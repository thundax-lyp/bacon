server:
  port: ${BACON_SERVER_PORT:8083}
  servlet:
    context-path: /api

spring:
  application:
    name: bacon-order-service
  main:
    banner-mode: off
  cloud:
    nacos:
      config:
        enabled: false
      discovery:
        enabled: ${BACON_NACOS_DISCOVERY_ENABLED:true}
        register-enabled: ${BACON_NACOS_REGISTER_ENABLED:true}
        server-addr: ${BACON_NACOS_SERVER_ADDR:127.0.0.1:8848}
        namespace: ${BACON_NACOS_NAMESPACE:public}
        group: ${BACON_NACOS_GROUP:DEFAULT_GROUP}
        username: ${BACON_NACOS_USERNAME:nacos}
        password: ${BACON_NACOS_PASSWORD:nacos}
  boot:
    admin:
      client:
        enabled: false

mybatis-plus:
  global-config:
    banner: false

bacon:
  runtime:
    mode: micro
  remote:
    order-base-url: ${BACON_REMOTE_ORDER_BASE_URL:http://bacon-order-service/api}
    order:
      provider-token: ${BACON_REMOTE_ORDER_PROVIDER_TOKEN:}
  web:
    internal-api:
      enabled: true
      header-name: X-Bacon-Provider-Token
      token: ${BACON_ORDER_PROVIDER_TOKEN:}
      include-path-patterns:
        - /providers/order/**

management:
  endpoints:
    web:
      exposure:
        include: health,info
