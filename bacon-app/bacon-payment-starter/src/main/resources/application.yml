server:
  port: ${BACON_SERVER_PORT:8085}
  servlet:
    context-path: /api

spring:
  application:
    name: bacon-payment-service
  main:
    banner-mode: off

mybatis-plus:
  global-config:
    banner: false

bacon:
  runtime:
    mode: micro
  remote:
    payment:
      provider-token: ${BACON_REMOTE_PAYMENT_PROVIDER_TOKEN:}
  web:
    internal-api:
      enabled: true
      header-name: X-Bacon-Provider-Token
      token: ${BACON_PAYMENT_PROVIDER_TOKEN:}
      include-path-patterns:
        - /providers/payment/**

management:
  endpoints:
    web:
      exposure:
        include: health,info
